version: 2

macros:
  - name: split_kind_col
    description: A macro to split a column with a "#" as a seperator
    arguments:
      - name: column_name
        type: string
        description: The name of the column you want to split